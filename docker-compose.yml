version: '3.8'
services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    ports:
      - '4444:4444'
    restart: unless-stopped
    networks:
      - zato-net

  ocr:
    build:
      context: .
      dockerfile: Dockerfile.ocr
    ports:
      - '5000:5000'
    restart: unless-stopped
    networks:
      - zato-net

networks:
  zato-net:
    driver: bridge
